import React from "react";
import { createRoot } from "react-dom/client";
import TradingDashboard from "./TradingDashboard.jsx";
createRoot(document.getElementById("root")).render(<TradingDashboard />);
