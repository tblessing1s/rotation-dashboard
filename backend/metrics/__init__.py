"""CFM numeric scorecard — a metrics-only lens that replaces manual chart-reading.

`scorecard` computes one numeric row per ticker (extension / weakness / volume /
trend / relative-strength metrics) plus a single composite verdict, so a fixed
holdings list can be judged for CFM suitability without eyeballing a chart.
"""
